declare module "html-space-cleaner" {
  /**
   * Normalizes HTML attributes and tags by cleaning up extra spaces
   * @param html The HTML string to normalize
   * @returns The normalized HTML string
   */
  export function normalizeAttributesAndTags(html: string): string;
}
